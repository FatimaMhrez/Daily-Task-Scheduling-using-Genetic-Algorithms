# Daily-Task-Scheduling-using-Genetic-Algorithms
A genetic algorithm implementation for daily task scheduling, considering task duration, priority, and dependencies. Features permutation encoding, tournament selection, order crossover, swap mutation, and a custom fitness function. Includes a Gradio-based UI for interactive parameter tuning and result visualization.
